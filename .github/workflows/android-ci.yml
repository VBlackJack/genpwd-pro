name: Android CI

on:
  push:
    branches: [ main, android/** ]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17
      - uses: android-actions/setup-android@v3
      - run: sdkmanager --install "platforms;android-35" "build-tools;35.0.0" "platform-tools"
      - run: ./gradlew :android:app:assembleDebug --no-daemon

  static-checks:
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Detekt (placeholder)
        run: echo "TODO: enable Detekt once configuration is ready"
      - name: ktlint (placeholder)
        run: echo "TODO: enable ktlint once configuration is ready"
      # - name: Android Lint (enable when SDK is available on CI runners)
      #   run: ./gradlew lint
